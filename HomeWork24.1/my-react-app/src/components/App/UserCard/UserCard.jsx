export default function Users({ user }) {
        return (
                <div>
                        <h2>Імя користувача: {user.name}</h2>
                        <h2>Хоббі: {user.hobby}</h2>


                </div>

        );

}
